# Errores Dev o Prod

``` javascript

const AppError = require('../utils/appError');

// MongoDB CastError (ID inválido)
const handleCastErrorDB = err => {
  const message = `Invalid ${err.path}: ${err.value}.`;
  return new AppError(message, 400);
};

// MongoDB Duplicate key (unique)
const handleDuplicateFieldsDB = err => {
  const value = err.errmsg.match(/(["'])(\\?.)*?\1/)[0];

  const message = `Duplicate field value: ${value}. Please use another value!`;
  return new AppError(message, 400);
};

// MongoDB Validation Error
const handleValidationErrorDB = err => {
  const errors = Object.values(err.errors).map(el => el.message);

  const message = `Invalid input data. ${errors.join('. ')}`;
  return new AppError(message, 400);
};

const sendErrorDev = (err, res) => {
  res.status(err.statusCode).json({
    status: err.status,
    error: err,
    message: err.message,
    stack: err.stack
  });
};

const sendErrorProd = (err, res) => {
  // Operational, trusted error: send message to client
  if (err.isOperational) {
    res.status(err.statusCode).json({
      status: err.status,
      message: err.message
    });
  } 
  // Programming or other unknown error: don't leak error details
  else {
    // 1) Log error
    console.error('ERROR 💥', err);

    // 2) Send generic message
    res.status(500).json({
      status: 'error',
      message: 'Something went very wrong!'
    });
  }
};

module.exports = (err, req, res, next) => {
  // console.log(err.stack);

  err.statusCode = err.statusCode || 500;
  err.status = err.status || 'error';

  if (process.env.NODE_ENV === 'development') {
    sendErrorDev(err, res);
  } else if (process.env.NODE_ENV === 'production') {
    let error = { ...err };

    if (error.name === 'CastError') 
      error = handleCastErrorDB(error);
    
    if (error.code === 11000) 
        error = handleDuplicateFieldsDB(error);

    if (error.name === 'ValidationError') 
      error = handleValidationErrorDB(error);

    sendErrorProd(error, res);
  }
};

```

# 1. `sendErrorDev` → modo desarrollo

``` javascript

const sendErrorDev = (err, res) => {
  res.status(err.statusCode).json({
    status: err.status,
    error: err,
    message: err.message,
    stack: err.stack
  });
};

```

## ¿Qué hace?

Devuelve TODOS los detalles del error:

- `status` → "fail" o "error"

- `error` → el objeto completo

- `message` → mensaje del error

- `stack` → [el stack trace](17.%20stackTrace.md)

## ¿Por qué esto es útil?

Porque cuando estamos desarrollando queremos ver:

```

Error: Cannot read property 'name' of undefined
    at userController.js:25:10

```

Eso viene de:

``` javascript

stack: err.stack

```

Esto nos dice:

- dónde ocurrió el error

- en qué archivo

- en qué línea

# 2. `sendErrorProd` → modo producción

``` javascript

const sendErrorProd = (err, res) => {

```

Aquí cambia completamente la filosofía

### Caso 1: error operacional (esperado)

``` javascript

if (err.isOperational) {
  res.status(err.statusCode).json({
    status: err.status,
    message: err.message
  });
}

```

### ¿Qué es un error operacional?

Errores que nosotros controlamos, por ejemplo:

- Usuario no encontrado

- Email inválido

- Token inválido

Ejemplo:

``` javascript

next(new AppError('User not found', 404));

```

Estos errores:

- son seguros

- podemos mostrarlos al cliente

### Caso 2: error desconocido (bug)

``` javascript

else {
  console.error('ERROR 💥', err);

  res.status(500).json({
    status: 'error',
    message: 'Something went very wrong!'
  });
}

```

### ¿Qué pasa aquí?

Si el error NO es operacional:

- Es probablemente un bug

Ejemplo:

- undefined

- null error

- fallo en lógica

- error inesperado

### ¿Por qué NO mostrar detalles?

Porque podríamos exponer cosas como:

- rutas del servidor

- queries de base de datos

- lógica interna

- credenciales (en algunos casos)

**Eso es un riesgo de seguridad**

# 3. El middleware global

``` javascript

module.exports = (err, req, res, next) => {

```

- Este es el **error handler global de Express**

### Paso 1: valores por defecto

``` javascript

err.statusCode = err.statusCode || 500;
err.status = err.status || 'error';

```

Si el error no tiene:

- `statusCode` → usa 500

- `status` → usa "error"

### Paso 2: detectar entorno

``` javascript

if (process.env.NODE_ENV === 'development') {
  sendErrorDev(err, res);
} else if (process.env.NODE_ENV === 'production') {
  sendErrorProd(err, res);
}

```

### ¿Qué es `NODE_ENV`?

Es una variable de entorno:

```

NODE_ENV=development
NODE_ENV=production

```

Define cómo se comporta la app

## Flujo completo

### 1. El controlador falla

``` javascript

throw new Error('Algo malo');

```

### 2. `catchAsync` lo captura

``` javascript

.catch(next);

```

### 3. Llega al middleware global

``` javascript

(err, req, res, next)

```

Se decide:

```

¿dev o prod?

```

Se envía respuesta adecuada

# Resumen

| Modo        | Qué muestra                  |
| ----------- | ---------------------------- |
| development | TODO (stack, error completo) |
| production  | Solo lo necesario (seguro)   |


## Idea clave

No todos los errores son iguales:

- **Operational (esperados)** → se muestran

- **Programming (bugs)** → se ocultan

# Funciones para errores que vienen de MongoBD

## Tabla de comparación

| Tipo de error     | Problema              | Resultado limpio               |
| ----------------- | --------------------- | ------------------------------ |
| [CastError](./07_MongoDB_Error/01.%20handleCastErrorDB.md)         | ID inválido           | Invalid _id: 123abc            |
| [Duplicate (11000)](./07_MongoDB_Error/02.%20handleDuplicateFieldsDB.md) | Campo único duplicado | Duplicate field value: "email" |
| [ValidationError](./07_MongoDB_Error/03.%20handleValidationErrorDB.md)   | Datos inválidos       | Invalid input data. ...        |

## Clave

Las 3 funciones hacen lo mismo:

```

Error técnico (Mongo/Mongoose)
        ↓
Función handler
        ↓
AppError (operational)
        ↓
Respuesta limpia al cliente

```