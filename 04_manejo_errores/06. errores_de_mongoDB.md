# Manejo de errores que vienen de la base de datos

Es el [errorController.js](./05.%20errorController.js.md) pero mejorado con la función para errores de MongoDB

``` javascript

const AppError = require('../utils/appError');

const handleCastErrorDB = err => {
  const message = `Invalid ${err.path}: ${err.value}.`;
  return new AppError(message, 400);
};

const sendErrorDev = (err, res) => {
  res.status(err.statusCode).json({
    status: err.status,
    error: err,
    message: err.message,
    stack: err.stack
  });
};

const sendErrorProd = (err, res) => {
  // Operational, trusted error: send message to client
  if (err.isOperational) {
    res.status(err.statusCode).json({
      status: err.status,
      message: err.message
    });
  } 
  // Programming or other unknown error: don't leak error details
  else {
    // 1) Log error
    console.error('ERROR 💥', err);

    // 2) Send generic message
    res.status(500).json({
      status: 'error',
      message: 'Something went very wrong!'
    });
  }
};

module.exports = (err, req, res, next) => {
  // console.log(err.stack);

  err.statusCode = err.statusCode || 500;
  err.status = err.status || 'error';

  if (process.env.NODE_ENV === 'development') {
    sendErrorDev(err, res);
  } else if (process.env.NODE_ENV === 'production') {
    let error = { ...err };

    if (error.name === 'CastError') 
      error = handleCastErrorDB(error);

    sendErrorProd(error, res);
  }
};

```

## 1. La función `handleCastErrorDB`

``` javascript
const handleCastErrorDB = err => {
  const message = `Invalid ${err.path}: ${err.value}.`;
  return new AppError(message, 400);
};

```

### ¿Qué es un `CastError`?

Es un error típico de **Mongoose** cuando le pasamos un valor inválido, por ejemplo:

```

GET /api/v1/users/123abc

```

Pero **Mongo** espera un **ObjectId** válido como:

```

507f1f77bcf86cd799439011

```

Entonces **Mongoose** lanza algo así:

``` javascript

{
  name: 'CastError',
  path: '_id',
  value: '123abc'
}

```

## ¿Qué hace la función?

``` javascript

const message = `Invalid ${err.path}: ${err.value}.`;

```

Construye un mensaje claro:

``` javascript

Invalid _id: 123abc.
return new AppError(message, 400);

```

Convierte el error de Mongo en:

- un error controlado (operational)

- código HTTP 400 (bad request)

Esto es CLAVE:

Estamos transformando un error técnico en uno entendible para el cliente.

# 2. Integración en el middleware global

``` javascript

else if (process.env.NODE_ENV === 'production') {
  let error = { ...err };

  if (error.name === 'CastError') 
    error = handleCastErrorDB(error);

  sendErrorProd(error, res);
}

```

## Paso a paso

### 1. Clonamos el error

``` javascript

let error = { ...err };

```

- Evitamos modificar el error original

Aunque aquí hay un detalle:

- esto es copia superficial (shallow copy)

- a veces `message` no se copia bien (detalle avanzado)

### 2. Detectamos el tipo de error

``` javascript

if (error.name === 'CastError')

```
Estamos diciendo:

- “Si este error viene de Mongo por un ID inválido…”

### 3. Lo transformamos

``` javascript

error = handleCastErrorDB(error);

```

Ahora error es un `AppError`

### 4. Lo mandamos al handler de producción

``` javascript

sendErrorProd(error, res);

```

### 5. Respuesta final:

``` json

{
  "status": "fail",
  "message": "Invalid _id: 123abc."
}

```

# ¿Por qué esto es tan importante?

Sin esto, el cliente vería algo como:

``` json

{
  "error": {
    "stringValue": "\"123abc\"",
    "kind": "ObjectId",
    ...
  }
}

```

- Confuso y poco profesional